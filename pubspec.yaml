name: transpayy
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  image_picker: ^1.1.2
  path: ^1.9.0
  flutter_tts: ^4.0.2
  flutter_local_notifications: ^21.0.0
  mobile_scanner: ^5.1.1 # Para usar la cámara y leer el QR

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/maps/route_passenger.png
    - assets/maps/route_driver.png
